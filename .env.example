# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real credentials.

# ClickUp Personal Access Token
# Used by /track-job skill to create tasks and set custom fields via REST API.
# Generate at: ClickUp → Settings → Apps → API Token
CLICKUP_ACCESS_TOKEN=your_clickup_personal_access_token_here
